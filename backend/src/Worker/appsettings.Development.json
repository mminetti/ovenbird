{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AzureBlobStorage": {
    // Azurite local storage emulator connection string - safe to commit, not a real credential.
    "ConnectionString": "UseDevelopmentStorage=true",
    "ContainerName": "market-documents"
  }
}
